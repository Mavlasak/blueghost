{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.surname) }}
        {{ form_row(form.phone) }}
        {{ form_row(form.email) }}
        {{ form_row(form.note) }}
        <input type="submit" value="Přiřadit" class="btn btn-success" />
    {{ form_end(form) }}

    <a href="{{ path('contact_delete', {id: contact.id}) }}">Smazat</a>
{% endblock %}
